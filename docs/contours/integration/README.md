# Контур интеграции с внешними системами

## 1. Назначение

Контур интеграции с внешними системами предназначен для получения, поиска и унификации сведений, необходимых для принятия решения и исполнения задач.

Его стратегическая цель состоит в предоставлении универсального внутреннего интерфейса для работы с внешними системами хранения задач, кода, документов, артефактов и коммуникаций.

## 2. Краткое описание

Что получает на вход:

- унифицированный внутренний запрос от других контуров;
- описание системы, объекта и операции;
- параметры чтения или записи.

Что отдаёт на выход:

- нормализованные данные внешней системы;
- результат внешнего действия;
- признаки недоступности, неполноты ответа или ошибки.

Как работает:

- выбирает нужный адаптер;
- преобразует внутренний запрос в вызов конкретной внешней системы;
- выполняет чтение или запись;
- нормализует ответ к единому контракту.

Чего не делает:

- не принимает решение, что именно должно происходить по рабочему циклу;
- не определяет инженерный смысл задачи;
- не подменяет собой контур исполнения.

## 3. Основной состав

В состав контура входят:

- диспетчер интеграционных модулей;
- адаптеры внешних систем;
- подсистема нормализации внешних объектов;
- хранилище приватных значений для данных доступа;
- подсистема кэширования и повторного использования контекста;
- подсистема контроля доступности и отказов.

## 4. Основные функции

- подключение к внешним API и служебным интерфейсам;
- поиск данных по объектам задачи;
- извлечение документов, параметров, состояний и артефактов;
- извлечение страниц документации из внешних хранилищ;
- кэширование повторно используемых сведений;
- унификация ответов разнородных источников;
- унификация команд записи и обратных действий во внешних системах;
- контроль полноты и доступности получаемой информации.

## 5. Входы и выходы

Входы:

- запросы на получение контекста;
- идентификаторы объектов и событий;
- указания от контура принятия решения;
- указания от контура исполнения.

Выходы:

- структурированные данные внешних систем;
- документы и артефакты;
- нормализованные представления объектов;
- признаки доступности или недоступности источников.

## 6. Внутренние элементы

### 6.1 Диспетчер интеграционных модулей

Диспетчер принимает внутренний запрос, определяет нужный адаптер, управляет политикой вызова, повторными попытками и нормализует итоговый результат к единому контракту.

Другие контуры должны видеть только этот единый контракт, а не различия между `gh`, HTTP API, webhook-ответами, файловыми хранилищами или мессенджерами.

### 6.2 Адаптер внешней системы

Адаптер инкапсулирует особенности конкретного источника: способ авторизации, формат вызова, набор поддерживаемых операций, преобразование ошибок и извлечение артефактов.

### 6.3 Нормализатор внешних объектов

Нормализатор переводит сущности внешней системы во внутренние типы комплекса: задачу, комментарий, запрос на слияние, ссылку на репозиторий, пользователя, состояние, документ и артефакт.

### 6.4 Подсистема кэширования

Подсистема кэширования удерживает результаты повторно используемых запросов в пределах жизненного цикла обработки, чтобы снизить число внешних обращений и сохранить воспроизводимость принятого решения.

### 6.5 Подсистема контроля доступности

Подсистема фиксирует недоступность источников, ошибки авторизации, превышение лимитов и неполноту ответа, чтобы другие контуры могли отличать отсутствие данных от отказа внешней системы.

### 6.6 Хранилище приватных значений

Хранилище приватных значений предоставляет адаптерам интеграции ключи и токены авторизации по имени, указанному в настройках интегрируемой системы. Реализация хранилища выбирается отдельной настройкой `private_store`; на macOS вариантом по умолчанию является macOS Keychain.

Публичное пространство имён контура интеграции использует типы `issue`, `repo`, `messenger` и `wiki`. Например, запрос задачи выполняется командой `progress integration issue get --id ABC-123`; конкретная интегрируемая система выбирается флагом `--system`, а без него — настройкой `default_systems.issue`. Значения `tracker` и `repository` сохраняются только для совместимого перехода настроек и сопровождаются диагностикой устаревшей формы.

Значение подставляется в адаптер в памяти процесса и не передаётся через переменные окружения.

## 7. Встроенные адаптеры

Практический срез контура поддерживает несколько встроенных интегрируемых систем.

GitHub подключается через штатную утилиту `gh` и работает в двух типах интеграции:

- трекер: получение задачи, получение комментариев задачи, создание комментария задачи, добавление и снятие меток задачи по каноническому названию;
- репозиторий: получение репозитория, получение запроса на слияние, создание запроса на слияние.

Bitbucket подключается через HTTP API и работает как репозиторий. Поддержаны варианты API Bitbucket Cloud и Bitbucket Server/Data Center:

- получение репозитория;
- получение запроса на слияние;
- создание запроса на слияние;
- получение замечаний запроса на слияние.

Mattermost подключается через HTTP API и работает как мессенджер:

- проверка авторизации;
- получение цепочки обсуждения по корневому сообщению;
- создание сообщения в канал или ответ в цепочку обсуждения.

Telegram подключается через Bot API и работает как мессенджер:

- проверка авторизации;
- создание сообщения в чат или ответа на сообщение.

Ограничение Telegram фиксируется явно: Bot API не даёт штатного чтения произвольной истории цепочки обсуждения, поэтому такая операция возвращает нормализованный отказ `unsupported-operation`.

Confluence подключается через HTTP API и работает как тип интеграции `wiki`. Практический срез ориентирован на локально размещённую версию Confluence Server/Data Center:

- проверка авторизации;
- получение страницы по идентификатору;
- поиск страниц по CQL;
- нормализация тела страницы, версии, пространства и ссылки.

Для HTTP-адаптеров поддержана ссылка `token_private` на хранилище приватных значений. Это позволяет хранить токены авторизации вне проектной конфигурации и не передавать их через переменные окружения.

## 8. Архитектурная роль

Контур выступает поставщиком сведений и исполнительным шлюзом во внешнюю среду. Он обеспечивает другие контуры данными из трекеров задач, документации, репозиториев и иных служебных систем, а также инкапсулирует операции записи, такие как комментарии, смена статуса или создание запроса на изменение.

При наличии встроенных адаптеров контур интеграции становится источником нормализованных сведений о задачах разработки, изменениях в коде и внешних обсуждениях, которые затем используются контуром принятия решения и контуром исполнения.

## 9. Дополнительная документация

Для разгрузки настоящего обзора детальные материалы вынесены в отдельные документы:

- `docs/contours/integration/TARGET_ARCHITECTURE.md` — целевое архитектурное состояние контура интеграции, его границы, основные сущности и взаимодействие с другими контурами.
- `docs/contours/integration/CONTRACTS.md` — ожидаемые внутренние контракты, классы действий, поведение и отказные состояния контура интеграции.
- `docs/contours/integration/CLI.md` — схема CLI-команд, конфигурация интегрируемых систем и порядок диагностических вызовов.
