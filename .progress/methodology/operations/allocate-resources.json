{
  "name": "allocate-resources",
  "kind": "allocate-resources",
  "title": "Ресурсное снабжение",
  "type": "builtin",
  "required": true,
  "contract": {
    "in": {
      "model_binding": {
        "type": "string",
        "required": true,
        "description": "Явная привязка модели выполнения к ресурсу."
      },
      "allow_model_fallback": {
        "type": "boolean",
        "required": true,
        "description": "Разрешение резервного выбора модели при недоступности привязки."
      },
      "runner": {
        "type": "string",
        "description": "Исполнительный модуль для запуска."
      },
      "model": {
        "type": "string",
        "description": "Модель выполнения."
      },
      "reasoning_effort": {
        "type": "string",
        "description": "Явно заданное усилие рассуждения модели."
      },
      "environment": {
        "type": "string",
        "description": "Среда выполнения."
      },
      "workplace_name": {
        "type": "string",
        "description": "Имя рабочего места для определения типа среды."
      },
      "repository_url": {
        "type": "string",
        "description": "Адрес репозитория для определения типа среды."
      }
    },
    "out": {
      "allocation": {
        "type": "object",
        "required": true,
        "description": "Результат ресурсного снабжения для последующих операций действия."
      }
    }
  }
}
