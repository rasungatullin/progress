{
  "name": "prepare-workplace",
  "kind": "prepare-workplace",
  "title": "Подготовка рабочего места",
  "origin": "builtin",
  "required": true,
  "contract": {
    "in": {
      "requires_workplace": {
        "type": "boolean",
        "required": true,
        "description": "Признак необходимости подготовки рабочего места для разрешённого действия."
      },
      "directory": {
        "type": "string",
        "description": "Каталог локального запуска."
      },
      "repository_url": {
        "type": "string",
        "description": "Адрес репозитория рабочего места."
      },
      "environment": {
        "type": "string",
        "description": "Выбранная среда выполнения."
      },
      "workplace_name": {
        "type": "string",
        "description": "Имя рабочего места."
      },
      "base_ref": {
        "type": "string",
        "description": "Базовая ветка рабочего места."
      },
      "head_ref": {
        "type": "string",
        "description": "Ветка рабочего места."
      },
      "allocated_environment": {
        "type": "string",
        "description": "Среда, выбранная ресурсным снабжением."
      },
      "allocated_environment_type": {
        "type": "string",
        "description": "Тип среды, выбранный ресурсным снабжением."
      },
      "action_name": {
        "type": "string",
        "required": true,
        "description": "Имя разрешённого действия для проверки ветки рабочего места."
      },
      "pull_request_base_ref": {
        "type": "string",
        "description": "Базовая ветка из данных запроса на слияние."
      },
      "pull_request_head_ref": {
        "type": "string",
        "description": "Ветка источника из данных запроса на слияние."
      }
    },
    "out": {
      "workplace": {
        "type": "object",
        "required": true,
        "description": "Подготовленное рабочее место для последующих операций действия."
      }
    }
  }
}
